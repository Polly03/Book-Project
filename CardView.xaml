<UserControl x:Class="BookDatabase.CardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookDatabase"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="100">
    <Border Background="LightPink"
            BorderBrush="#CCCCCC"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Image Grid.Row="0"
                   Source="https://via.placeholder.com/150"  Stretch="Uniform"
                   Margin="5"/>

            <StackPanel Grid.Row="1" Margin="10,0,10,10">
                <TextBlock Text="Card Title" 
                           FontSize="14" 
                           FontWeight="Bold" 
                           TextWrapping="Wrap"
                           Margin="0,0,0,5"/>
                <TextBlock Text="This is a short description of the card content." 
                           FontSize="12" 
                           Foreground="Gray"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
