<Window x:Class="BookDatabase.test"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="test" Height="720" Width="1280">

    <Grid>
        
        
        
        <!-- HLAVNÍ OBSAH -->
        <ScrollViewer>
            <WrapPanel>
                <!-- tady budou kartičky knih -->
            </WrapPanel>
        </ScrollViewer>

        <!-- MALÉ MENU (60px) -->
        <StackPanel x:Name="SmallMenu" Background="LightGray" Width="60" HorizontalAlignment="Left" VerticalAlignment="Stretch">
            <Button Content="≡" Background="Gray" FontSize="24" Click="ToggleMenu"/>
            <Button Content="📚" Background="Gray"  FontSize="24" ToolTip="Knihy" Margin="0,10,0,0"/>
            <Button Content="👤" Background="Gray"  FontSize="24" ToolTip="Autoři" Margin="0,10,0,0"/>
            <Button Content="➕" Background="Gray"  FontSize="24" ToolTip="Přidat knihu" Margin="0,10,0,0"/>
        </StackPanel>

        <!-- VELKÉ MENU (240px) -->
        <Border x:Name="LargeMenu" Background="Gray" Width="240"
        HorizontalAlignment="Left" VerticalAlignment="Stretch"
        Visibility="Collapsed">
            <StackPanel Margin="10">
                <Button Content="≡" FontSize="24" Click="ToggleMenu"/>
                <Button Content="Knihy" FontSize="24" ToolTip="Knihy" Margin="0,10,0,0"/>
                <Button Content="Autoři" FontSize="24" ToolTip="Autoři" Margin="0,10,0,0"/>
                <Button Content="Přidat" FontSize="24" ToolTip="Přidat knihu" Margin="0,10,0,0"/>
                <Separator Margin="0,10,0,10"/>
                <TextBlock Text="Filtry:" FontWeight="Bold"/>


                <CheckBox Content="česko" Margin="0,10,0,0"></CheckBox>
                <CheckBox Content="česko" Margin="0,10,0,0"></CheckBox>
                <CheckBox Content="česko" Margin="0,10,0,0"></CheckBox>
                <CheckBox Content="česko" Margin="0,10,0,0"></CheckBox>


                

                <!-- sem vlož žánry, jazyky, nakladatele -->
            </StackPanel>
        </Border>
    </Grid>
</Window>

