<UserControl x:Class="BookDatabase.CardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookDatabase"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="150">
    
    <Border Background="AliceBlue"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>


            <StackPanel Grid.Row="1" Margin="10,10,10,10">
               
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="14" FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,0,5"/>
                <TextBlock Text="{Binding Author, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="12" FontWeight="Bold" TextWrapping="Wrap" Height="28"/>
                <TextBlock Text="{Binding Genre, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="12" FontWeight="Bold" TextWrapping="Wrap" Height="43"/>

            </StackPanel>
        </Grid>
    </Border>
</UserControl>
