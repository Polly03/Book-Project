<UserControl x:Class="BookDatabase.BooksWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookDatabase"
        mc:Ignorable="d"  
        Height="auto" Width="auto">

    <ScrollViewer>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" ></ColumnDefinition>
                <ColumnDefinition Width="auto"></ColumnDefinition>
                <ColumnDefinition Width="auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Menu Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3" Padding="100,10,100,10">
                <Menu.ItemsPanel>
                    <ItemsPanelTemplate>
                        <DockPanel/>
                    </ItemsPanelTemplate>
                </Menu.ItemsPanel>

                <MenuItem FontSize="35" Width="130" Height="60">
                    <MenuItem.Header>
                        <TextBlock Text="Knihy" VerticalAlignment="Center" Height="60" Width="123"/>
                    </MenuItem.Header>
                </MenuItem>

                <MenuItem FontSize="35" Width="130">
                    <MenuItem.Header>
                        <TextBlock Text="Autoři" VerticalAlignment="Center" Height="60" Width="121"/>
                    </MenuItem.Header>
                </MenuItem>

                <MenuItem FontSize="35" Width="200">
                    <MenuItem.Header>
                        <TextBlock Text="Vyhledat" VerticalAlignment="Center" Height="60" Width="200"/>
                    </MenuItem.Header>
                </MenuItem>
                <MenuItem FontSize="25" Width="250">
                    <MenuItem.Header>
                        <TextBox Text="Vyhledat sem" VerticalAlignment="Center" Height="72" Width="244"></TextBox>
                    </MenuItem.Header>
                </MenuItem>
            </Menu>
            <StackPanel Grid.Row="1" Grid.Column="0" Grid.RowSpan="3" MinHeight="500">
                <Button  FontSize="25" Click="Button_Click">Přidat knihu</Button>
                <Button  FontSize="25">Filtrovat</Button>
                <Button  FontSize="25">seřadit</Button>
            </StackPanel>

            <ScrollViewer VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Disabled" Grid.Column="1" Grid.ColumnSpan="3" Grid.Row="1" Grid.RowSpan="3">
                <ItemsControl ItemsSource="{Binding MyItems}" >
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel x:Name="MyPanel"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- Define how each item should look -->
                            <Border Margin="5" Padding="10">
                                <local:CardView Height="{Binding Height}" Width="{Binding Width}"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
          

        </Grid>
    </ScrollViewer>




</UserControl>
